export type CreatedProjects = {
    name: string
    icon: string
    description: string
    githublink: string
}

export type Experiences = {
    title: string
    date: string
    content: string
}